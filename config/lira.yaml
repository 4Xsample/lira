# LIRA Configuration
# Placeholder for future settings
api:
  port: 1312
  host: "0.0.0.0"

logging:
  level: "info"
  file: "/var/log/lira.log"

# Model configuration
model:
  # The main model to be used by the orchestrator
  # This model should be available in Ollama
  main_model: "gemma2:9b"